import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pollingowl.app',
  appName: 'ProyectoProgramacionMobile',
  webDir: 'dist/ProyectoProgramacionMobile/browser'
};

export default config;
